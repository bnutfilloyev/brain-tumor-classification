# Production frontend image — serves a pre-built dist (built on CI/dev to keep
# the small server's RAM free). Run `npm run build` before deploying.
FROM nginx:alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
